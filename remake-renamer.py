import os
import sys

args = sys.argv
print(args)

# Receive commandline parameters


# Check for valid syntax. We need a file path and new name, if no valid syntax, end program and give correct syntax


# Loop trough all files in file path, recursively

    # Rename to have the new name with same file extension
    # Print the old and new name

